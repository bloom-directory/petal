package bloom:env@0.1.0;

interface runtime {
  now-ms: func() -> result<u64, string>;
  random-bytes: func(len: u32) -> result<list<u8>, string>;
  setting: func(key: string) -> result<option<string>, string>;
}
